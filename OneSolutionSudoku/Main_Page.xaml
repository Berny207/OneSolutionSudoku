<Page x:Class="OneSolutionSudoku.Main_Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OneSolutionSudoku"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" d:DesignWidth="800"
      Title="Page1" Height="526">
    <Viewbox Stretch="Uniform">
    <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        <Label Name="Label_Title" Style="{StaticResource DefaultLabel}" HorizontalAlignment="Center" VerticalAlignment="Top" FontWeight="Black" Grid.Row="0"/>
            <Button Name="Button_Generation" Click="ButtonGenerateClick" Style="{StaticResource RoundedButton}" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="1"/>
            <Button Name="Button_OwnCreation" Click="ButtonOwnCreationClick" Style="{StaticResource RoundedButton}" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="2"/>
            <Button Name="Button_Options" Click="ButtonOptionsClick" Style="{StaticResource RoundedButton}" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="3"/>
            <Button Name="Button_End" Click="ButtonEndClick" Style="{StaticResource RoundedButton}" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="4"/>
    </Grid>
    </Viewbox>
</Page>
